{
  "name": "hcig-passport",
  "version": "1.0.0",
  "private": true,
  "description": "Business case and interface library for HCIG Passport — a group-wide patient identity and care app for Healthcare International Group.",
  "scripts": {
    "build": "node build.js",
    "build:artifacts": "node build.js --inline",
    "dev": "node build.js && node serve.js"
  },
  "engines": {
    "node": ">=18"
  }
}
